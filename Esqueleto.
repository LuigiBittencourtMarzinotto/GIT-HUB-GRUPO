Soma(elemento1, elemento2)
  retornar a soma
divisao (elemento1, elemento2)
  retornar a divisao
subtracao (elemento1, elemento2)
  retornar a subtracao
multiplicacao (elemento1, elemento2)
  retornar a multiplicacao
var
n1, n2 : real

funcao f_somar (p_n1, p_n2 : real) : real
var
n1, n2, soma : real
inicio
escreval("Digite o 1o número: ")
leia(n1)
escreval("Digite o 2o número: ")
leia(n2)
soma <- n1 + n2
retorne soma
fimfuncao

inicio 
escreval("A soma dos números é: ", f_somar(n1,n2))
fimalgoritmo
