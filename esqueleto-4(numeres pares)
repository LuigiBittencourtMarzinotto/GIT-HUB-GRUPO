var
vet: vetor[0..9] de inteiro
numero, i : inteiro
inicio
// Seção de Comandos
para i <- 0 ate 9 passo 1 faca
     escreval("Informe um número: ")
     leia(vet[i])
     se vet[i] mod 2 = 0 entao
        escreval("O número ", vet[i], "é par")
        senao
        escreval("Não é Par")
     fimse
fimpara
fimalgoritmo
